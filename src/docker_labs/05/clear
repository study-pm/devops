#!/bin/bash

### DESCRIPTION: Clears all of the output generated by running the start command

source VARS.sh

# Remove the containers
./rm-cont.sh "${LABEL}-exec"
./rm-cont.sh "${LABEL}-override"
./rm-cont.sh "${LABEL}-shell"

# Remove the images
./rm-img.sh "$REPO:v1"
./rm-img.sh "$REPO:v2"

# List the Container (must be empty)
./list.sh

# Verify the Images (must be empty)
./verify.sh
