#!/bin/bash

### DESCRIPTION: Clears all of the output generated by running the start command

source .env

# Remove the images
./rm-img.sh nginx:alpine
./rm-img.sh mysql:5.7

# Verify the Images (must be empty)
./verify.sh nginx alpine
./verify.sh mysql 5.7
